# Pipeline dependencies (analysis brain). Node/Remotion deps are in package.json.
numpy>=1.24
pillow>=10.0
imageio-ffmpeg>=0.5     # bundles a static ffmpeg binary (no system install)

# Local VLM (Ollama) needs NO python package — it is called over its local HTTP
# API using only the standard library. Just install the Ollama app + a model:
#   winget install Ollama.Ollama
#   python -m pipeline.run setup --pull --test

# Optional: only needed if you set ANTHROPIC_API_KEY to enable CLOUD multimodal
# tagging + critique. The pipeline runs fully without it (and without any cloud).
anthropic>=0.40 ; python_version >= "3.8"
